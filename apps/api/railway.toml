[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter=api run build"
watchPatterns = ["apps/api/**", "packages/**"]

[deploy]
startCommand = "pnpm --filter=api run db:migrate && pnpm --filter=api run start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
